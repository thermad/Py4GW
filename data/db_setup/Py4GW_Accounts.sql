--
-- File generated with SQLiteStudio v3.4.21 on Sun Jun 21 00:45:08 2026
--
-- Text encoding used: System
--
PRAGMA foreign_keys = off;
BEGIN TRANSACTION;

-- Table: Account
CREATE TABLE IF NOT EXISTS Account (ID INTEGER PRIMARY KEY ON CONFLICT ROLLBACK AUTOINCREMENT UNIQUE NOT NULL, Email TEXT NOT NULL UNIQUE, Password TEXT NOT NULL, is_password_encrypted INTEGER DEFAULT (0) NOT NULL, GW_Client_Path TEXT NOT NULL, Extra_Args TEXT, run_as_admin INTEGER DEFAULT (0) NOT NULL, inject_py4gw INTEGER DEFAULT (1) NOT NULL, inject_gmod INTEGER DEFAULT (0) NOT NULL, gmod_mods TEXT, startup_script TEXT, HWND NUMERIC);

-- Table: Character
CREATE TABLE IF NOT EXISTS Character (ID INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, AccountID INTEGER NOT NULL REFERENCES Account (ID), Name TEXT NOT NULL, Profession TEXT, Level INTEGER);

-- Table: Team
CREATE TABLE IF NOT EXISTS Team (ID INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, TeamID INTEGER REFERENCES Team_Name (ID) NOT NULL, CharacterID INTEGER REFERENCES Character (ID) NOT NULL);

-- Table: Team_Name
CREATE TABLE IF NOT EXISTS Team_Name (ID INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, Name TEXT NOT NULL);

COMMIT TRANSACTION;
PRAGMA foreign_keys = on;
