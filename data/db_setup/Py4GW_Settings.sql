--
-- File generated with SQLiteStudio v3.4.21 on Sun Jun 21 13:55:43 2026
--
-- Text encoding used: System
--
PRAGMA foreign_keys = off;
BEGIN TRANSACTION;

-- Table: Globals
CREATE TABLE IF NOT EXISTS Globals (ID INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, WidgetID INTEGER REFERENCES Widget (ID) NOT NULL, Section TEXT NOT NULL, Key TEXT NOT NULL, KeyType TEXT NOT NULL, Value TEXT NOT NULL, UNIQUE (WidgetID, Section, Key));

-- Table: Keys
CREATE TABLE IF NOT EXISTS Keys (ID INTEGER PRIMARY KEY AUTOINCREMENT, WidgetID INTEGER REFERENCES Widget (ID) NOT NULL, AccountID INTEGER NOT NULL, Section TEXT NOT NULL, Key TEXT NOT NULL, KeyType TEXT NOT NULL, Value TEXT NOT NULL, UNIQUE (WidgetID, AccountID, Section, Key));

-- Table: Widget
CREATE TABLE IF NOT EXISTS Widget (ID INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, Key TEXT NOT NULL UNIQUE, Name TEXT NOT NULL, Description TEXT);

COMMIT TRANSACTION;
PRAGMA foreign_keys = on;
